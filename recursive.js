function countDown(number) {
	for (let i = number; i > 0; i--) {
		console.log(i);
	}	
}
countDown(10)

/*

create function countDown using the parameter variable of (number) {
	for (if number is greater than 0 then do the following and -1 from number) {
		then print the current value
	}	
}
parameter number for function countDown(is 10)

*/